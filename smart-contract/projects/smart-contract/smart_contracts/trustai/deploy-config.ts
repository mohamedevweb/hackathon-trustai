import { ContractDeployConfig } from "algokit"

const deployConfig: ContractDeployConfig = {
  contract: "trustai/contract.algo.ts",
  network: "testnet",
  name: "trustai_contract",
}

export default deployConfig
